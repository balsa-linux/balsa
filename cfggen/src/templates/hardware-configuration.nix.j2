# Stands in for nixos-generate-config; disko owns the filesystems.
{ config, lib, modulesPath, ... }:

{
  imports = [ (modulesPath + "/installer/scan/not-detected.nix") ];

  boot.initrd.availableKernelModules = [
    "xhci_pci"
    "ahci"
    "nvme"
    "usbhid"
    "usb_storage"
    "sd_mod"
    # From nixos-generate-config's qemu-guest profile; a VM initrd needs them to see its disk.
    "virtio_pci"
    "virtio_blk"
    "virtio_scsi"
  ];
  boot.initrd.kernelModules = [ ];
{%- if hardware.cpu == "intel" %}
  boot.kernelModules = [ "kvm-intel" ];
{%- elif hardware.cpu == "amd" %}
  boot.kernelModules = [ "kvm-amd" ];
{%- else %}
  boot.kernelModules = [ ];
{%- endif %}
  boot.extraModulePackages = [ ];

  nixpkgs.hostPlatform = lib.mkDefault "{{ hardware.arch }}";
}
