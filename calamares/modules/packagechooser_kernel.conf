---
mode: required
method: legacy

labels:
    step: "Kernel"
    # reason: "Choose the Linux kernel this install will boot."  # unread by 3.4.2; kept for a C++ screen

items:
- id: "default"
  name: "Default (7.2)"
  description: "Current mainline stable. linuxPackages_latest. Recommended unless you need something specific."

- id: "lts"
  name: "LTS (6.18)"
  description: "Long-term support mainline. linuxPackages. Fewer surprises, slower to get new hardware support."

- id: "zen"
  name: "Zen (x86_64 only)"
  description: "Desktop-responsiveness patch set. [Not available on ARM]"

- id: "xanmod"
  name: "Xanmod (x86_64 only)"
  description: "Throughput and latency tuning over mainline. [Not available on ARM]"

- id: "cachyos"
  name: "CachyOS (x86_64 only)"
  description: "CachyOS patches and tunings, tracking latest. First boot may take a while if the binary cache misses. [Not available on ARM]"

- id: "cachyos-lts"
  name: "CachyOS LTS (x86_64 only)"
  description: "CachyOS patches on the LTS base. First boot may take a while if the binary cache misses.  [Not available on ARM]"

- id: "cachyos-bore"
  name: "CachyOS BORE (x86_64 only)"
  description: "CachyOS with the BORE scheduler, based on latest. First boot may take a while if the binary cache misses.  [Not available on ARM]"
